{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || { echo '[agent-hook] failed to resolve project root' >&2; exit 2; }; exec \"$root/scripts/agent-hook.sh\" pre-push",
            "timeout": 900,
            "statusMessage": "Running pre-push quality gate"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "root=\"$(git rev-parse --show-toplevel)\" || { echo '[agent-hook] failed to resolve project root' >&2; exit 2; }; exec \"$root/scripts/agent-hook.sh\" format",
            "timeout": 600,
            "statusMessage": "Formatting repository"
          }
        ]
      }
    ]
  }
}
