{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "grep -F 'git push' >/dev/null || exit 0; make -C \"${CLAUDE_PROJECT_DIR}\" check 1>&2 || exit 2",
            "timeout": 900,
            "statusMessage": "Checking pre-push quality gate"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "make -C \"${CLAUDE_PROJECT_DIR}\" fmt 1>&2 || exit 2",
            "timeout": 600,
            "statusMessage": "Formatting repository"
          }
        ]
      }
    ]
  }
}
